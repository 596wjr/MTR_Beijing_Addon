/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * Learn more about Gradle by exploring our Samples at https://docs.gradle.org/8.10/samples
 */

plugins {
    id 'java'
//    id 'net.minecraftforge.gradle' version '6.0.25'
}

def modName = 'MTR Beijing Addon'
def modVersion = '0.0.5'

repositories {
    flatDir { dirs 'libs' }
    mavenCentral()

    maven { url = "https://maven.aliyun.com/repository/central" }

    // ======= MC 专用 =======
    maven {
        name = "BMCLAPI"
        url = "https://bmclapi2.bangbang93.com/maven"
        allowInsecureProtocol = true
        content {
            includeGroup "net.minecraft"
            includeGroup "net.fabricmc"
            includeGroup "net.minecraftforge"
            includeGroup "dev.architectury"
            includeGroup "org.parchmentmc"
            includeGroup "me.shedaniel"
            includeGroup "com.terraformersmc"
            includeGroup "maven.modrinth"
        }
    }
}

dependencies {
    compileOnly fileTree(dir: 'libs', include: ['*.jar'])
    compileOnly 'com.google.code.gson:gson:2.8.9'
//    compileOnly fg.deobf("net.minecraftforge:forge:40.2.0")
    // Forge 的 @Mod 注解类（net.minecraftforge.fml.common.Mod）由 javafmllanguage 提供，
    // 已放入 libs/（javafmllanguage-1.20.1-47.2.0.jar），由上面的 fileTree 自动引入。
    // 不要依赖 maven 坐标 'net.minecraftforge:forge:...' —— 其 maven 产物不含 FML 类，
    // 且本模组为纯 API 模组，编译期只需 @Mod 注解，无需完整 Forge/MC classpath。
//    compileOnly 'net.minecraftforge:forge:1.18.2-40.2.0'
}

java {
    sourceCompatibility = JavaVersion.VERSION_17
    targetCompatibility = JavaVersion.VERSION_17
}

processResources {
    duplicatesStrategy = DuplicatesStrategy.INCLUDE
}

// 配置打包任务
jar {
    from sourceSets.main.output
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE

    filesMatching('fabric.mod.json') {
        expand(
                'version': modVersion
        )
    }
    filesMatching('META-INF/mods.toml') {
        expand(
                'version': modVersion
        )
    }

    // 输出文件名：MTR Beijing Addon-1.0.0.jar
    archiveBaseName = modName
    archiveVersion = modVersion
    manifest {
        attributes 'Implementation-Title': modName,
                'Implementation-Version': modVersion
    }
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}